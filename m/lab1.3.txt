#include <iostream>
#include <iomanip>
#include <string>
namespace std;
int main() {  
  int hours=10800; // 3*60*60
  int sec=60;//amount of seconds in one minute    
  int min1=3*sec;// This is a variable to hold the value of 3 minutes in seconds
  int min2=5*sec;// the value of 5 minutes in seconds    
  float pi=3.141526;      
  cout << "result:  " << hours << endl;  
}